// const express = require('express');
// const { sendToToken, sendToTopic, subscribeToken } = require('../Controllers/send_notifications_controller');

// const router = express.Router();

// const authMiddleware = require("../Middlewares/auth_middleware");



// Everything below this line is authenticated (protected routes)
// router.use(authMiddleware);

// router.post('/notify/token', sendToToken);
// router.post('/notify/topic', sendToTopic);
// router.post('/topic/subscribe', subscribeToken);

// module.exports = router;
